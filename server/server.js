Meteor.startup(function() {
  // if (Topics.find().count() === 0) {
    // var titles = ["Ada Lovelace", "Grace Hopper", "Marie Curie", "Carl Friedrich Gauss", "Nikola Tesla", "Claude Shannon"];
    // for (var i = 0; i < titles.length; i++)
      // Topics.insert({
        // title : titles[i],
        // content : titles[i],
      // });
  // }
  // Topics.remove({});
}); 